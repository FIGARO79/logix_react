# Configuración de seguridad para la aplicación Logix API Router
# Copiar este archivo a .env y actualizar con valores seguros en producción

# Clave secreta para firmar tokens y sesiones (mínimo 32 caracteres aleatorios)
SECRET_KEY=Figaro1979*

# Contraseña de administrador para operaciones críticas (actualización de BD, etc.)
UPDATE_PASSWORD=Figaro1979*

# IMPORTANTE:
# - En producción, genera claves seguras aleatorias
# - Nunca compartas estos valores en control de versiones
# - Mantén el archivo .env fuera del repositorio (añádelo a .gitignore)

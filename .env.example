# IMPORTANTE: Copia este archivo como .env y reemplaza TODOS los valores
# NO SUBAS el archivo .env al repositorio

# =============================================================================
# CONFIGURACIÓN DE SEGURIDAD (OBLIGATORIA)
# =============================================================================

# Clave secreta para sesiones (OBLIGATORIA)
# Esta clave se usa para firmar y encriptar sesiones de usuario
# Genera una con: python -c "import secrets; print(secrets.token_urlsafe(32))"
# NUNCA uses la misma clave en desarrollo y producción
SECRET_KEY=GENERA_UNA_CLAVE_SEGURA_AQUI

# Contraseña de administrador para actualización de archivos (OBLIGATORIA)
# Usa una contraseña fuerte y única (mínimo 12 caracteres, letras, números y símbolos)
UPDATE_PASSWORD=DEFINE_PASSWORD_SEGURO_AQUI

# =============================================================================
# CONFIGURACIÓN ADICIONAL (Opcional)
# =============================================================================

# Entorno de ejecución (development/production)
# ENVIRONMENT=development

# Modo debug (solo para desarrollo, NUNCA en producción)
# DEBUG=False

# Configuración de base de datos (por defecto usa la ruta configurada en config.py)
# DB_PATH=inbound_log.db

# Configuración de archivos CSV (por defecto usa la carpeta configurada en config.py)
# CSV_FOLDER=databases

# =============================================================================
# NOTAS DE SEGURIDAD
# =============================================================================
# 1. Genera claves únicas para cada entorno (desarrollo, pruebas, producción)
# 2. Rota las claves periódicamente (cada 3-6 meses)
# 3. Si sospechas que una clave fue comprometida, cámbiala inmediatamente
# 4. Nunca compartas tu archivo .env por correo, chat o repositorios
# 5. En producción, considera usar servicios de gestión de secretos

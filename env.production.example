# Configuración de producción para PythonAnywhere
# IMPORTANTE: Copia este archivo como .env y reemplaza TODOS los valores con valores seguros

# Dominio de PythonAnywhere (reemplaza YOUR_USERNAME)
PYTHONANYWHERE_DOMAIN=YOUR_USERNAME.pythonanywhere.com

# Entorno
ENVIRONMENT=production

# =============================================================================
# CONFIGURACIÓN DE SEGURIDAD (OBLIGATORIA)
# =============================================================================

# Clave secreta para sesiones (OBLIGATORIA - GENERA UNA NUEVA Y SEGURA)
# NUNCA uses valores por defecto o de ejemplo en producción
# Genera una con: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=GENERA_UNA_CLAVE_SEGURA_AQUI

# Password para actualización de archivos (OBLIGATORIA - CAMBIA ESTO)
# Usa una contraseña fuerte: mínimo 12 caracteres, letras, números y símbolos
UPDATE_PASSWORD=DEFINE_PASSWORD_SEGURO_AQUI

# =============================================================================
# CONFIGURACIÓN DE APLICACIÓN
# =============================================================================

# Configuración de base de datos (SQLite)
# La ruta será relativa al proyecto en PythonAnywhere
DB_PATH=inbound_log.db

# Configuración de archivos CSV
CSV_FOLDER=databases

# Modo debug (debe ser False en producción)
DEBUG=False

# =============================================================================
# NOTAS DE SEGURIDAD PARA PRODUCCIÓN
# =============================================================================
# 1. NUNCA uses las mismas claves que en desarrollo
# 2. Genera claves únicas para cada entorno
# 3. Restringe el acceso al archivo .env
# 4. Rota las claves periódicamente (cada 3-6 meses)
# 5. Si las claves fueron comprometidas, cámbialas inmediatamente
# 6. No compartas este archivo por correo, chat o repositorios

